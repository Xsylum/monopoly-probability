public class CircularArray<T> {
	
	// Needs to hold either a tile or integer, so it can hold the number of landings
	// other objects need to be able to interact, finding the next "tile" if they move(x tiles);
	// I think circular works well because the board loops, but maybe there is a better way to store? (this allows different sized monopoly boards though)
	
}